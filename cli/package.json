{
  "name": "rune-cli",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "license": "MIT",
  "bin": {
    "rune": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "postbuild": "cp -r src/templates dist/ && node -e \"const fs=require('fs');const f='dist/index.js';const c=fs.readFileSync(f,'utf8');fs.writeFileSync(f,'#!/usr/bin/env node\\\\n'+c.replace('#!/usr/bin/env node\\\\n',''))\""
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
